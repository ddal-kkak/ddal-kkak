import { PiCursorClick } from 'react-icons/pi'
import { ReactIconsType } from './type'

export default function IconClickOutline({ className = 'text-2xl' }: ReactIconsType) {
  return <PiCursorClick className={className} />
}
